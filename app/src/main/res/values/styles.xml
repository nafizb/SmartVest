<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>
    <style name="ThermometerView">
        <item name="viewBg">@color/bgColor</item>
        <!-- <item name="minScaleLineColor">@color/colorPrimaryDark</item> -->
        <item name="scaleTextSize">21sp</item>
        <item name="unitTextSize">19sp</item>
        <item name="maxScaleLineColor">@color/black</item>
        <item name="midScaleLineColor">@color/black</item>
        <item name="leftMercuryColor">@color/red</item>
        <item name="rightMercuryColor">@color/red</item>

    </style>
</resources>
